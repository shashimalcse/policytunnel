{
  "name": "policytunnel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:libs": "pnpm run --parallel  --filter \"@policytunnel/core\" build",
    "test:unit": "pnpm build:libs && pnpm run --parallel --filter \"@policytunnel/core\" test:unit",
    "lint": "pnpm build:libs && pnpm run --parallel --filter \"@policytunnel/core\" lint"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.3.0",
    "assemblyscript": "^0.27.9",
    "ts-jest": "^29.1.1",
    "ts-patch": "^3.0.1",
    "tsconfig-paths": "^4.2.0",
    "ttypescript": "^1.5.15",
    "typescript": "^5.1.3",
    "typescript-transform-paths": "^3.4.6"
  },
  "dependencies": {
    "jest": "^29.6.0"
  }
}
